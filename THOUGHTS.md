- [x] When first booting the Device drop down selects the first device but it isn't connected at this point, on load this list should show a - at the top, and this should be selected (you shouldnt be able to do anything with a - selected)
- [x] When writing a file to a device, it would be good to know that this file has changed significantly from the previous config on the device. Maybe auto back up the config before updating, use a name for the backup that includes the Name, serial, v/p id.
- [x] The axis config right hand section for each row, do the following: Use center, change this to Center, align it before the checkbox to match Min/Max, move the set center button next to the center checkbox, make it square add and Icon to it instead of text. Reset Calibration button moves to the old space used for Set Center, Use and icon instead of text. This will all tidy up this area.
- [x] When an axis is selected in axes curves, the thumbnail graph is totally blue, you cant see it, change the selected state indicator. If an axis is not output, when selecting the Axes curves tab, that axis should show an indicator that is is not in use. If I toggle the output state in axes config, this should update the disabled state on axes curve.
